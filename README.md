# 前端刷题系统

基于 Vue 3 + TypeScript + Vite 构建的刷题应用

## 功能特性

- 📚 题库选择（按分类、难度筛选）
- ✍️ 在线答题（单选/多选）
- 📊 结果统计（正确率、用时、错题回顾）
- 💾 数据驱动（只需更新 data.json）
- 📱 响应式设计

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 项目结构

```
src/
├── views/          # 页面组件
│   ├── Home.vue    # 首页
│   ├── Quiz.vue    # 答题页
│   └── Result.vue  # 结果页
├── composables/    # 组合式函数
│   └── useQuiz.ts  # 答题逻辑
├── types/          # TypeScript 类型
├── router/         # 路由配置
└── main.ts         # 入口文件

data/
└── data.json       # 题目数据
```

## 添加题目

编辑 `data/data.json` 文件，按照以下格式添加：

```json
{
  "category": "javascript",
  "type": "single",
  "question": "题目内容",
  "options": ["选项A", "选项B", "选项C", "选项D"],
  "answer": 0,
  "explanation": "解析内容",
  "difficulty": "easy",
  "status": "active"
}
```

## 部署

```bash
npm run build
```

将 `dist` 文件夹上传到静态服务器即可。

更新题目时，只需替换服务器上的 `data/data.json` 文件。
